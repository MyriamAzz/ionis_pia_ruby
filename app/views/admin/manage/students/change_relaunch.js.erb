<% if @comment %>
$("#modal").html("<%= escape_javascript(render 'change_relaunch') %>")
$('#modal').modal('show')
<% else %>
$('[data-toggle="tooltip"]').tooltip('hide')
$("#btn_header_edit").html(
    "<%= escape_javascript(render partial: 'admin/manage/students/btn_header_edit') %>"
  );
  $('[data-toggle="tooltip"]').tooltip()
  $("#kt_datatable_stats_students").KTDatatable("reload");

<% end %>